select
  id,
  title,
  features,
  is_paused,
  paused_at,
  api_key_id,
  area_codes,
  batch_size,
  created_at,
  creator_id,
  is_started,
  archived_at,
  archived_by,
  description,
  is_archived,
  ingest_method,
  organization_id,
  texting_hours_end,
  batch_size_replies,
  messaging_strategy,
  request_after_reply,
  texting_hours_start,
  release_idle_replies,
  texting_hours_enforced,
  texting_hours_timezone,
  use_dynamic_assignment,
  is_permanently_archived,
  release_idle_replies_time,
  use_dynamic_assignment_replies,
  override_organization_area_codes,
  override_organization_texting_hours,
  _cta_loaded_at
from {{ source('cta', 'campaigns_base') }}

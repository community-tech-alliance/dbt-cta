select
    selected_voterbase_id,
    user_last_name,
    created_at,
    source,
    contact_id,
    duration,
    reference,
    van_id,
    user_first_name,
    updated_at,
    stopped_reason,
    reported,
    activity_id,
    activity_published_at,
    id,
    stopped_at,
    first_name,
    campaign_id,
    activity_title,
    user_email,
    twilio_answered_by,
    last_name,
    answered_at,
    exported_at,
    pdi_id,
    phone,
    user_id,
    _airbyte_emitted_at,
    _airbyte_v2_voice_calls_export_hashid
from {{ source('cta','v2_voice_calls_export_base') }}
select
    performs,
    created_at,
    completions,
    description,
    started,
    folder_name,
    impressions,
    type,
    title,
    seen,
    exported_at,
    privacy_setting,
    updated_at,
    contact_list_id,
    contact_list_name,
    activity_id,
    clicks,
    id,
    aasm_state,
    folder_id,
    published_at,
    campaign_id,
    _airbyte_emitted_at,
    _airbyte_activities_export_hashid
from {{ source('cta','activities_export_base') }}
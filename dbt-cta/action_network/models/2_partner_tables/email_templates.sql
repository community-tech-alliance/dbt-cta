select
    _airbyte_emitted_at,
    id,
    name,
    uuid,
    notes,
    footer,
    header,
    user_id,
    group_id,
    created_at,
    syndicated,
    updated_at,
    from_suffix,
    unsubscribe,
    logo_file_name,
    logo_file_size,
    logo_dimensions,
    logo_content_type
from {{ source('cta','email_templates_base') }}
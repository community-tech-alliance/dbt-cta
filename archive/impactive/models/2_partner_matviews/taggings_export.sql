select
    reportable_id,
    voterbase_id,
    reported_by_email,
    tag_name,
    tag_slug,
    created_at,
    exported_at,
    reportable_type,
    van_id,
    updated_at,
    phone,
    user_id,
    tag_id,
    id,
    fullname,
    taggable_id,
    reported_by_fullname,
    value,
    email,
    campaign_id,
    taggable_type,
    _airbyte_emitted_at,
    _airbyte_taggings_export_hashid
from {{ source('cta','taggings_export_base') }}